# NewsC 在共用 stock-pg 内的库凭据（勿提交真密）
# 云端实际密码写在本机 /tmp/newsc-deploy/pg_pass.txt 与远端 /opt/newsc/deploy/.env.pg
POSTGRES_USER=newsc
POSTGRES_PASSWORD=change-me
POSTGRES_DB=newsc

# 共用容器名（勿改，除非 stock 侧同步改名）
SHARED_PG_CONTAINER=stock-pg
